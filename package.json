{
  "name": "websocket-chat-app",
  "version": "1.0.0",
  "description": "A WebSocket chat app using ws and an HTTP server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "ws": "^8.12.0"
  }
}
